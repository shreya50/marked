[package]
name = "marked-rs"
version = "0.1.0"
edition = "2021"
description = "A small, fast Markdown-to-HTML parser written in Rust"
license = "MIT"

[lib]
name = "marked_rs"
path = "src/lib.rs"

[[bin]]
name = "marked-rs"
path = "src/main.rs"

[profile.release]
lto = true
codegen-units = 1
